spring:
  main:
    allow-bean-definition-overriding: true
  security:
    user:
      name:
      password:
      roles:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:
  redis:
    host:
    port:
    password:
    timeout:
  rabbitmq:
    host:
    port:
    username:
    password:
    virtual-host:

server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
